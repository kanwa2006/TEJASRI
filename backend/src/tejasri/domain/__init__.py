"""Domain layer: entities, value objects, and interfaces.

Pure Python only — no framework or infrastructure imports. Everything
below `application/` and `infrastructure/` depends on this package,
never the other way around.
"""
